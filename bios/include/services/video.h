#pragma once

#include <stdint.h>
#include <stddef.h>
#include <stdbool.h>

#include "interrupt.h"

/*
void video_set_page(bioscall __seg_ss* const regs);
//void video_set_mode(bioscall __seg_ss* const regs);
void video_set_palette(bioscall __seg_ss* const regs);

void video_write_char_only(bioscall __seg_ss* const regs);
void video_write_char_attr(bioscall __seg_ss* const regs);
void video_write_char_tty(bioscall __seg_ss* const regs);

void video_set_cursor_type(bioscall __seg_ss* const regs);
void video_set_cursor_pos(bioscall __seg_ss* const regs);

void video_scroll_wnd_up(bioscall __seg_ss* const regs);
void video_scroll_wnd_down(bioscall __seg_ss* const regs);

void video_read_at_cursor(bioscall __seg_ss* const regs);

void video_get_state(bioscall __seg_ss* const regs);
void video_get_cursor(bioscall __seg_ss* const regs);
void video_get_lightpen_pos(bioscall __seg_ss* const regs);

void video_pixel_read(bioscall __seg_ss* const regs);
void video_pixel_write(bioscall __seg_ss* const regs);
*/